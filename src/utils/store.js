// store.js
import { ref } from "vue";

export const items = ref([]);
export const count = ref(0);
export const selectedIndex = ref(null);
export const popup = ref(false);
export const popupIdx = ref(null);
